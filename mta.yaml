ID: strapiapp1
_schema-version: "2.1"
version: 0.0.1
   
build-parameters:
  before-all:
   - builder: custom
     commands:
      - npm install
      - npm run build
      - npm install --production


modules:
  - name: strapiapp1
    path: .
    type: nodejs
    parameters:
      memory: 1024MB
      disk-quota: 1024MB