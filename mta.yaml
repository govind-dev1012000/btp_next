ID: nextapp1
_schema-version: "3.0"
version: 0.0.1
   
build-parameters:
  before-all:
   - builder: custom
     commands:
      - npm install
      - npm run build
      - npm install --production


modules:
  - name: nextapp1
    path: ./btp_next
    type: nodejs
    parameters:
      memory: 1024MB
      disk-quota: 1024MB