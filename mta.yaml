ID: nextapp101
_schema-version: "3.0"
version: 0.0.1
   
build-parameters:
  before-all:
   - builder: custom
     commands:
      - npm install
      - npm run build


modules:
  - name: nextapp101
    path: .
    type: nodejs
    parameters:
      memory: 1024MB
      disk-quota: 1024MB