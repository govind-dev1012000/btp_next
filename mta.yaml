ID: nextappmta10
_schema-version: "2.1"
version: 0.0.1
   
build-parameters:
  before-all:
   - builder: custom
     commands:
      - npm install
      - npm run build


modules:
  - name: nextappmta10
    path: .
    type: nodejs
    parameters:
      memory: 256M
      disk-quota: 512M
    # build-parameters:
    #   build-result: dist
    #   builder: custom
    #   commands:
    #   - npm install
    #   - npm run build
    #   supported-platforms: []

# - name: nextappmta1
#   type: nodejs
#   path: .
#   parameters:
#     memory: 256M
#     disk-quota: 512M
#   build-parameters:
#     build-result: dist
#     builder: custom
#     commands:
#     - npm install
#     - npm run build
#     supported-platforms: []

      